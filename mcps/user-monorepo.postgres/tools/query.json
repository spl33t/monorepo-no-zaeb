{
  "name": "query",
  "description": "Выполняет read-only SQL к подключённой базе PostgreSQL (транзакция READ ONLY). Вход: sql — текст запроса.",
  "arguments": {
    "type": "object",
    "properties": {
      "sql": {
        "description": "SQL-запрос для выполнения",
        "type": "string"
      }
    },
    "required": ["sql"],
    "additionalProperties": false
  }
}
